diff --git a/third_party/blink/renderer/core/timezone/timezone_controller.cc b/third_party/blink/renderer/core/timezone/timezone_controller.cc
index 8e4166b61c218..91c5da10d7d67 100644
--- a/third_party/blink/renderer/core/timezone/timezone_controller.cc
+++ b/third_party/blink/renderer/core/timezone/timezone_controller.cc
@@ -4,7 +4,9 @@

 #include "third_party/blink/renderer/core/timezone/timezone_controller.h"

+#include "base/command_line.h"
 #include "base/feature_list.h"
+#include "base/no_destructor.h"
 #include "mojo/public/cpp/bindings/pending_remote.h"
 #include "mojo/public/cpp/bindings/remote.h"
 #include "third_party/blink/public/common/thread_safe_browser_interface_broker_proxy.h"
@@ -105,6 +107,32 @@ bool SetIcuTimeZoneAndNotifyV8(const String& timezone_id) {

 }  // namespace

+std::unique_ptr<TimeZoneController::TimeZoneOverride>&
+CommandLineTimezoneOverride() {
+  static base::NoDestructor<std::unique_ptr<TimeZoneController::TimeZoneOverride>>
+      handle;
+  return *handle;
+}
+
+void ApplyCommandLineTimezoneOverride() {
+  const base::CommandLine* command_line =
+      base::CommandLine::ForCurrentProcess();
+  if (!command_line) {
+    return;
+  }
+  const std::string timezone_value =
+      command_line->GetSwitchValueASCII("timezone");
+  if (timezone_value.empty()) {
+    return;
+  }
+  auto result =
+      TimeZoneController::SetTimeZoneOverride(String::FromUtf8(timezone_value));
+  if (result.status == TimeZoneController::TimeZoneOverrideStatus::kSuccess &&
+      result.handle) {
+    CommandLineTimezoneOverride() = std::move(result.handle);
+  }
+}
+
 TimeZoneController::TimeZoneController() {
   DCHECK(IsMainThread());
   if (!base::FeatureList::IsEnabled(kLazyBlinkTimezoneInit)) {
@@ -123,6 +151,7 @@ void TimeZoneController::Init() {
   Platform::Current()->GetBrowserInterfaceBroker()->GetInterface(
       monitor.BindNewPipeAndPassReceiver());
   monitor->AddClient(instance().receiver_.BindNewPipeAndPassRemote());
+  ApplyCommandLineTimezoneOverride();
 }

 // static
